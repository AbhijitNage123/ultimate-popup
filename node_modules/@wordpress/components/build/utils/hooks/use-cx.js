"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useCx = void 0;

var _react = require("@emotion/react");

var _utils = require("@emotion/utils");

var _css = require("@emotion/css");

var _element = require("@wordpress/element");

/**
 * External dependencies
 */
// eslint-disable-next-line no-restricted-imports
// eslint-disable-next-line no-restricted-imports

/**
 * WordPress dependencies
 */
// @ts-ignore Private property
const EmotionCacheContext = _react.CacheProvider._context;

const useEmotionCacheContext = () => (0, _element.useContext)(EmotionCacheContext);

const isSerializedStyles = o => ['name', 'styles'].every(p => typeof o[p] !== 'undefined');
/**
 * Retrieve a `cx` function that knows how to handle `SerializedStyles`
 * returned by the `@emotion/react` `css` function in addition to what
 * `cx` normally knows how to handle. It also hooks into the Emotion
 * Cache, allowing `css` calls to work inside iframes.
 *
 * @example
 * import { css } from '@emotion/react';
 *
 * const styles = css`
 * 	color: red
 * `;
 *
 * function RedText( { className, ...props } ) {
 * 	const cx = useCx();
 *
 * 	const classes = cx(styles, className);
 *
 * 	return <span className={classes} {...props} />;
 * }
 */


const useCx = () => {
  const cache = useEmotionCacheContext();
  const cx = (0, _element.useCallback)((...classNames) => {
    return (0, _css.cx)(...classNames.map(arg => {
      if (isSerializedStyles(arg)) {
        (0, _utils.insertStyles)(cache, arg, false);
        return `${cache.key}-${arg.name}`;
      }

      return arg;
    }));
  }, [cache]);
  return cx;
};

exports.useCx = useCx;
//# sourceMappingURL=use-cx.js.map