"use strict";

var _interopRequireWildcard = require("@babel/runtime/helpers/interopRequireWildcard");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useCardDivider = useCardDivider;

var _css = require("@emotion/css");

var _element = require("@wordpress/element");

var _context = require("../../ui/context");

var styles = _interopRequireWildcard(require("../styles"));

/**
 * External dependencies
 */
// Disable reason: Temporarily disable for existing usages
// until we remove them as part of https://github.com/WordPress/gutenberg/issues/30503#deprecating-emotion-css
// eslint-disable-next-line no-restricted-imports

/**
 * WordPress dependencies
 */

/**
 * Internal dependencies
 */

/**
 * @param {import('../../ui/context').PolymorphicComponentProps<import('../../divider').DividerProps, 'hr', false>} props
 */
function useCardDivider(props) {
  const {
    className,
    ...otherProps
  } = (0, _context.useContextSystem)(props, 'CardDivider');
  const classes = (0, _element.useMemo)(() => (0, _css.cx)(styles.Divider, styles.borderColor, // This classname is added for legacy compatibility reasons.
  'components-card__divider', className), [className]);
  return { ...otherProps,
    className: classes
  };
}
//# sourceMappingURL=hook.js.map